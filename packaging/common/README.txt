Dataplicity Lens is a coherent, modern toolkit for understanding Linux systems.

This package contains lens-top, a read-only process explorer. Run `man lens-top` or
`lens-top --help` for usage. No account, daemon, telemetry or root privilege is required.
