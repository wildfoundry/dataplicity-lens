[package]
name = "lens-diagnostics"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
lens-history = { version = "=0.2.0", path = "../history" }
lens-model = { version = "=0.2.0", path = "../model" }
